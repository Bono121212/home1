import React from 'react'
import {Container, Checkbox, Grid, Header, List, Menu, Radio, Segment, Select, Sticky, Table,} from 'semantic-ui-react'
import {Link} from 'react-router-dom'


const Check = () => (
    <Checkbox
        placeholder='분류를 선택해주세요'
        label="체크박스"
    />
);

export default Check